import numpy as np 
import pandas as  pd 
import matplotlib as plt
np.random.seed()
# 一个seed值对应一个随机数，设置一下后失效
np.random.random()
#产生一个随机数
np.linspace(start,stop,sequenceSize=50)
#产生一个等差数列
np.random.normal(from prml.features import PolynomialFeatures
from prml.linear import (
    LinearRegressor,
    RidgeRegressor,
    BayesianRegressor
))
